import { IsbnDbLinkPipe } from './isbn-db-link.pipe';

describe('IsbnDbLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new IsbnDbLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
